alias cdo='cd $OLDPWD'
alias r='reset'
alias a='ls -l'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Git aliases (Kept here so that all aliases are in one place)
alias s='git status'
alias glog='git log --all --graph'
